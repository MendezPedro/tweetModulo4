<h2 class="pb-5">Registrarse</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="form-group">
    <%= f.label :email, 'Su email' %><br />
    <%= f.email_field :email, autocomplete: "email", class: "form-control", placeholder: "ingrese su email" %>
  </div>

  <div class="form-group">
    <%= f.label :name, 'Nombre' %><br />
    <%= f.text_field :name, autocomplete: "name", class: "form-control", placeholder: "ingrese su nombre" %>
  </div>

  <div class="form-group">
    <%= f.label :photo, 'Avatar' %><br />
    <%= f.text_field :photo, autocomplete: "photo", class: "form-control", placeholder: "ingrese link de su foto" %>
  </div>

  <div class="form-group">
    <%= f.label :password %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> characters minimum)</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "new-password", class: "form-control", placeholder: "ingrese su contraseña" %>
  </div>

  <div class="form-group">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control", placeholder: "confirme su contraseña" %>
  </div>

  <div class="field text-center">
    <%= f.button "Nueva sesion", class:"px-3 mx-5 btn btn-primary text-center" %>
  </div>
<% end %>


